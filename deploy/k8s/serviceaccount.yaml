apiVersion: v1
kind: ServiceAccount
metadata:
  name: tessera
  namespace: tessera
  labels:
    app.kubernetes.io/name: tessera
# The pod needs its token to call the Kubernetes API (read-only — see rbac.yaml).
automountServiceAccountToken: true
